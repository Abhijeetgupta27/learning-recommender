apiVersion: v1

kind: PersistentVolume

metadata:
  name: postgres-pv

spec:
  storageClassName: ""

  capacity:
    storage: 1Gi

  accessModes:
    - ReadWriteOnce

  hostPath:
    path: "/data/postgres"